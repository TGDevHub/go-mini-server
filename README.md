# go-mini-server

Учебный HTTP-сервер на Go, разработанный с фокусом на чистоту архитектуры, безопасную обработку запросов и расширяемость.

Проект разработан в учебных целях. Цель — не production-ready сервер, а понимание устройства серверной части Go-приложений.

## 🧠 Цель

Научиться:

- Строить минимальный HTTP-сервер на Go.
- Работать с middleware, контекстами, логами.
- Писать unit-тесты и обрабатывать паники.
- Готовить код к расширению и масштабированию.

## 🚀 Реализовано

- Простая HTTP-обёртка с маршрутизацией.
- Контроллер `POST /user/get` с JSON-ответом.
- Panic recovery + логирование.
- Поддержка middleware.
- Graceful shutdown через `http.Server.Shutdown()`.
- Юнит-тест хендлера.
- Конфигурация через YAML.

## 🔧 Пример запроса

```http
POST http://localhost:8080/user/get
Content-Type: application/json

{
  "id": 0
}
```
Ответ 

```json
{
  "status": "success",
  "data": {
    "id": 0,
    "name": "Name",
    "second_name": "Second",
    "position": "CEO"
  }
}
```

## Как расширять
- Добавить уровни service/repository.
- Интегрировать MySQL, PostgreSQL или SQLite.
- Генерация OpenAPI/Swagger схем.
- Подключить Kafka, Redis и т. д.

📦 Структура
```
cmd/server/         # Точка входа
internal/web/       # HTTP-сервер, middleware, обработчики
core/               # Утилиты и общие функции
config.yaml         # Конфигурация сервера
```